# Data folder (not tracked by git)

Put your raw exports here (e.g., Strong CSV). This repo does not store private data.

Expected inputs (example):
- data/strong_workouts.csv
- data/sleep.csv (optional)
- data/recovery.csv (optional)

Only this README is committed.
